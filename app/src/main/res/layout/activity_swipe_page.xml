<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:arc="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/parent_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_feeds"
    android:orientation="vertical"
    android:paddingEnd="8dp"
    android:paddingStart="7dp"
    android:paddingTop="10dp">

    <include
        android:id="@+id/feed_header"
        layout="@layout/header_football_feeds"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <include
        android:id="@+id/feed_info"
        layout="@layout/layout_info_football_feeds"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/feed_header" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/football_feed_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/feed_info"
        android:layout_marginEnd="2dp"
        android:layout_marginStart="2dp"
        android:paddingTop="5dp"
        android:paddingBottom="50dp"
        android:clipToPadding="false"
        android:background="@color/background_color"
        tools:listitem="@layout/football_feed_row"
        arc:layoutManager="android.support.v7.widget.GridLayoutManager"
        arc:spanCount="1" />

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

    <com.bvapp.arcmenulibrary.ArcMenu
        android:id="@+id/arc_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        arc:menuChildAnim="false"
        arc:menuClickAnim="true"
        arc:menuGravity="Bottom_Middle"
        arc:menuImage="@drawable/ic_un"
        arc:menuNormalColor="@color/fab_button_pink"
        arc:menuShadowElevation="true"
        arc:menuType="normal" />
</RelativeLayout>
